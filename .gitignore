# Local credentials — copy the .example files in config/
config/db.php
config/mail.php
.env

# Composer dependencies (restore with: composer install)
/vendor/

# Uploaded user content
/uploads/avatars/*
/uploads/grounds/*
!/uploads/avatars/.gitkeep
!/uploads/grounds/.gitkeep

# WebP migration backups (old originals kept locally)
/uploads/_legacy_backup/

# Logs & OS cruft
*.log
Thumbs.db
.DS_Store
~$*

# Local design docs & low-fi mockups (keep off GitHub)
Proposal.docx
/futsal_low/
prompt.txt
DESIGN.md
design.docx
PRODUCT.md

# Local lab work (keep off GitHub)
/js(lab1)/
lab1.pdf

# Local tooling & scratch dirs (kept off GitHub)
/.opencode/
/.impeccable/

# Deployment build output
/dist/

# Local dev preview helper (localhost-only previews)
tools/preview.php

# Editor folders
.idea/
.vscode/
